# 数字唱名练习

选择练习数字、每组长度和是否允许连续重复，按顺序读出随机数字串的唱名，再查看答案自行核对。修改设置会生成新题并隐藏答案。

如果交互练习暂时无法加载，可以先读这一组：`1 3 5 2 4 6 7 1`。

读完后核对：`do mi sol re fa la si do`。

本练习采用 `1 do、2 re、3 mi、4 fa、5 sol、6 la、7 si` 的对应关系，不判断音高或节奏。
